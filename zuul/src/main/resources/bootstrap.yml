eureka:
  client:
    service-url:
      defaultZone: http://localhost:8768/eureka/
server:
  port: 9003
spring:
  application:
    name: zuul
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev
